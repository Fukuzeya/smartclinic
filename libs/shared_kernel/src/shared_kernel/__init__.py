"""SmartClinic shared kernel.

Re-exports are intentionally minimal: callers should import from the
sub-packages (`shared_kernel.domain`, `shared_kernel.types`, ...) so the
dependency direction between layers remains obvious.
"""

__version__ = "0.1.0"
